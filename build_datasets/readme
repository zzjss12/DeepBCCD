
## If you want to examine the code of other projects, refer to the following procedure：
1.Reference vulseeker(https://github.com/buptsseGJ/VulSeeker)

2.Replace the 2_gen_features.py file in the build_datasets directory with the 2_gen_features.py file in vulseeker.

3.vulseker can extract the binary function collection to be detected and store it in json file.

4.The training set and test set are extracted from the json file using the drug_datasets_from_json.py file.



## If you just want to rebuild the training and test sets using the data sets I've provided, follow the procedure below:
The training and test sets are extracted from the json(two Jsons are provided) files using the drug_datasets_from_json.py file.



